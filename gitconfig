[user]
    name = kelvin
    email = kowochen@gmail.com
[merge]
    tool = extMerge
[mergetool "extMerge"]
    cmd = extMerge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
    trustExitCode = false
[diff]
    external = extDiff
[core]
    gitproxy = gitproxy
[color]
	ui = true
