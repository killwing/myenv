[user]
    name = kelvin
    email = kowochen@gmail.com
[merge]
    tool = extMerge
[mergetool "extMerge"]
    cmd = extMerge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
    trustExitCode = false
[diff]
    external = extDiff
[color]
    ui = true
[alias]
    lg = log --graph --oneline --all
	st = status
	co = checkout
	ci = commit
	br = branch
    sm = submodule
